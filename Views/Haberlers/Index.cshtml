@model IEnumerable<WebProje.Models.Haberler>

@using Microsoft.AspNetCore.Builder
@using Microsoft.AspNetCore.Localization
@using Microsoft.Extensions.Options
@using Microsoft.AspNetCore.Mvc.Localization

@inject IViewLocalizer Localizer

@inject IOptions<RequestLocalizationOptions> Locoptions
@{
    var requestCulture = Context.Features.Get<IRequestCultureFeature>();
    var cultureItems = Locoptions.Value.SupportedCultures
        .Select(c => new SelectListItem { Value = c.Name, Text = c.DisplayName })
        .ToList();
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Russo+One&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Ubuntu&display=swap" rel="stylesheet">

    <link href="~/css/pilotlar.css" rel="stylesheet" />
   
    
</head>
<body>
    <form id="cultureForm" asp-action="Index">
        <select name="culture" onchange="this.form.submit()" asp-for="@requestCulture.RequestCulture.UICulture.Name" asp-items="cultureItems">
        </select>
    </form>
    <div id="govdeTakim">

        <div id="habersol">
            <div id="demo" class="carousel slide" data-ride="carousel">
                <ul class="carousel-indicators">
                    <li data-target="#demo" data-slide-to="0" class="active"></li>
                    <li data-target="#demo" data-slide-to="1"></li>
                    <li data-target="#demo" data-slide-to="2"></li>
                    <li data-target="#demo" data-slide-to="3"></li>
                    <li data-target="#demo" data-slide-to="4"></li>
                    <li data-target="#demo" data-slide-to="5"></li>
                </ul>
                <div class="carousel-inner">
                    <div class="carousel-item active">
                        <img src="~/img/Haber1.jpg" alt="Los Angeles" id="haberfoto">
                        <div class="carousel-caption">
                            <h3>
                                @foreach (var item in Model)
                                {
                                    if (item.Id == 1)
                                    {
                                        <a asp-route-id=1 asp-controller="Haberlers" asp-action="Details" style="color:white;">
                                            @if (requestCulture.RequestCulture.Culture.ToString() == "en-US")
                                            {
                                                @Html.DisplayFor(modelItem => item.BaslikEng)

                                            }
                                            else
                                            {
                                                @Html.DisplayFor(modelItem => item.Baslik)
                                            }
                                        </a>
                                    }

                                }
                            </h3>

                        </div>
                    </div>
                    <div class="carousel-item">
                        <img src="~/img/Haber2.jpg" alt="Los Angeles" id="haberfoto">
                        <div class="carousel-caption">
                            <h3>
                                @foreach (var item in Model)
                                {
                                    if (item.Id == 2)
                                    {
                                        <a asp-route-id=2 asp-controller="Haberlers" asp-action="Details" style="color:white;">
                                            @if (requestCulture.RequestCulture.Culture.ToString() == "en-US")
                                            {@Html.DisplayFor(modelItem => item.BaslikEng)}
                                        else
                                        {
                                            @Html.DisplayFor(modelItem => item.Baslik)
                                        }
                                        </a>
                                    }

                                }
                            </h3>

                        </div>
                    </div>
                    <div class="carousel-item">
                        <img src="~/img/Haber3.jpg" alt="Los Angeles" id="haberfoto">
                        <div class="carousel-caption">
                            <h3>
                                @foreach (var item in Model)
                                {
                                    if (item.Id == 3)
                                    {
                                        <a asp-route-id=3 asp-controller="Haberlers" asp-action="Details" style="color:white;">
                                            @if (requestCulture.RequestCulture.Culture.ToString() == "en-US")
                                            {@Html.DisplayFor(modelItem => item.BaslikEng)}
                                        else
                                        {
                                            @Html.DisplayFor(modelItem => item.Baslik)
                                        }
                                        </a>
                                    }

                                }
                            </h3>

                        </div>
                    </div>
                    <div class="carousel-item">
                        <img src="~/img/Haber4.jpg" alt="Los Angeles" id="haberfoto">
                        <div class="carousel-caption">
                            <h3>
                                @foreach (var item in Model)
                                {
                                    if (item.Id == 4)
                                    {
                                        <a asp-route-id=4 asp-controller="Haberlers" asp-action="Details" style="color:white;">
                                            @if (requestCulture.RequestCulture.Culture.ToString() == "en-US")
                                            {@Html.DisplayFor(modelItem => item.BaslikEng)}
                                        else
                                        {
                                            @Html.DisplayFor(modelItem => item.Baslik)
                                        }
                                        </a>
                                    }

                                }
                            </h3>

                        </div>
                    </div>
                    <div class="carousel-item">
                        <img src="~/img/Haber5.jpg" alt="Los Angeles" id="haberfoto">
                        <div class="carousel-caption">
                            <h3>
                                @foreach (var item in Model)
                                {
                                    if (item.Id == 5)
                                    {
                                        <a asp-route-id=5 asp-controller="Haberlers" asp-action="Details" style="color:white;">
                                            @if (requestCulture.RequestCulture.Culture.ToString() == "en-US")
                                            {@Html.DisplayFor(modelItem => item.BaslikEng)}
                                        else
                                        {
                                            @Html.DisplayFor(modelItem => item.Baslik)
                                        }
                                        </a>
                                    }

                                }
                            </h3>

                        </div>
                    </div>
                    <div class="carousel-item">
                        <img src="~/img/Haber6.jpg" alt="Los Angeles" id="haberfoto">
                        <div class="carousel-caption">
                            <h3>
                                @foreach (var item in Model)
                                {
                                    if (item.Id == 6)
                                    {
                                        <a asp-route-id=6 asp-controller="Haberlers" asp-action="Details" style="color:white;">
                                            @if (requestCulture.RequestCulture.Culture.ToString() == "en-US")
                                            {@Html.DisplayFor(modelItem => item.BaslikEng)}
                                        else
                                        {
                                            @Html.DisplayFor(modelItem => item.Baslik)
                                        }

                                        </a>
                                    }

                                }
                            </h3>

                        </div>
                    </div>
                </div>
                <a class="carousel-control-prev" href="#demo" data-slide="prev">
                    <span class="carousel-control-prev-icon"></span>
                </a>
                <a class="carousel-control-next" href="#demo" data-slide="next">
                    <span class="carousel-control-next-icon"></span>
                </a>
            </div>
        </div>


        <div id="habersag">
            <div id="habersagust">
                <h3>@Localizer["TÜM HABERLER"]</h3>

            </div>

            @foreach (var item in Model)
            {



                <div id="habersag1">
                    @if (item.Id == 1)
                    {
                        <a asp-route-id=1 asp-controller="Haberlers" asp-action="Details" style="color:black;">
                            @if (requestCulture.RequestCulture.Culture.ToString() == "en-US")
                            {@Html.DisplayFor(modelItem => item.BaslikEng)}
                        else
                        {
                            @Html.DisplayFor(modelItem => item.Baslik)
                        }
                        </a>
                    }
                    @if (item.Id == 2)
                    {
                        <a asp-route-id=2 asp-controller="Haberlers" asp-action="Details" style="color:black;">
                            @if (requestCulture.RequestCulture.Culture.ToString() == "en-US")
                            {@Html.DisplayFor(modelItem => item.BaslikEng)}
                        else
                        {
                            @Html.DisplayFor(modelItem => item.Baslik)
                        }
                        </a>
                    }
                    @if (item.Id == 3)
                    {
                        <a asp-route-id=3 asp-controller="Haberlers" asp-action="Details" style="color:black;">

                            @if (requestCulture.RequestCulture.Culture.ToString() == "en-US")
                            {@Html.DisplayFor(modelItem => item.BaslikEng)}
                        else
                        {
                            @Html.DisplayFor(modelItem => item.Baslik)
                        }
                        </a>
                    }
                    @if (item.Id == 4)
                    {
                        <a asp-route-id=4 asp-controller="Haberlers" asp-action="Details" style="color:black;">
                            @if (requestCulture.RequestCulture.Culture.ToString() == "en-US")
                            {@Html.DisplayFor(modelItem => item.BaslikEng)}
                        else
                        {
                            @Html.DisplayFor(modelItem => item.Baslik)
                        }
                        </a>
                    }
                    @if (item.Id == 5)
                    {
                        <a asp-route-id=5 asp-controller="Haberlers" asp-action="Details" style="color:black;">
                            @if (requestCulture.RequestCulture.Culture.ToString() == "en-US")
                            {@Html.DisplayFor(modelItem => item.BaslikEng)}
                        else
                        {
                            @Html.DisplayFor(modelItem => item.Baslik)
                        }
                        </a>
                    }
                    @if (item.Id == 6)
                    {
                        <a asp-route-id=6 asp-controller="Haberlers" asp-action="Details" style="color:black;">
                            @if (requestCulture.RequestCulture.Culture.ToString() == "en-US")
                            {@Html.DisplayFor(modelItem => item.BaslikEng)}
                        else
                        {
                            @Html.DisplayFor(modelItem => item.Baslik)
                        }
                        </a>
                    }


                </div>
            }
        </div>

    </div>

</body>
</html>
